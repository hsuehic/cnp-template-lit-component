image: node:16

stages:
  - publish

pages:
  stage: publish
  cache:
    paths:
    - node_modules/

  script:
    - npm install
    - npm run build-storybook:gitlab
  artifacts:
    paths:
    - public
  
  only:
    - tags 
  except:
    - branches
